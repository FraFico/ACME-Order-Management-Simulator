# Power BI — screenshots

Add the dashboard screenshots here (PNG), then reference them from the main `README.md`.

Suggested files:
- `dashboard-overview.png` — KPI row + monthly revenue
- `revenue-by-category.png`
- `top-customers.png`

The `.pbix` file itself is **not** committed (see `.gitignore`) because it can be large.
If you want to version it, use [Git LFS](https://git-lfs.com/); otherwise the screenshots
plus the notebook are enough to tell the story.
